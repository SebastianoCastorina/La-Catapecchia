@model IEnumerable<La_Catapecchia.Models.Servizi>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdServizio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomeServizio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CostoServizio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdServizio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomeServizio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CostoServizio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.IdServizio }) 
           
            @Html.ActionLink("Delete", "Delete", new { id=item.IdServizio  })
        </td>
    </tr>
}

</table>
